2. Процессы и потоки ( потоки - обшая память )
  2.1 как они могут работатть одновременно
  2.2 GREEN THREADS ( в какой версии java они были )

3. Многопоточность в Java
    - как создавать потоки
    - что за состояние Waiting ?
  3.1 Volatile
  3.2 средства синхронизации потоков
      -synchronized
      class ....
        Object lock = new Obcject();
        void m1(){
          synch(lock){
            // ...... do smth
          }
        }
        void m2(){
           lock.hashCode();
           
        }    
     }
     
     если первый поток вызвал метод m1 и заснул там, а потом второй поток вызвал метод m2
     что будет? второй поток будет ждать пока первы не закончит работу или ?
     
